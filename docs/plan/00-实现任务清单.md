# 实现任务清单 —— 从现状到满足 20 项需求

> 基线日期:2026-06-08(以代码实测为准);勾选进度更新于 2026-06-11(P0 地基 + #6 质量评估完成);
> **2026-06-15 再更新**:算子市场 + 加工/质量整页编辑器 + DeepSeek AI 接入 + 整套容器化部署(详见 §一末「2026-06-15 增量」)。
> 对照需求 [`数据工程&数据集.md`](../requirements/数据工程&数据集.md) 与能力映射 [`08-需求映射.md`](../data-engineering/08-需求映射.md)。
> 领域模型与分层以 [`03-架构设计.md`](03-架构设计.md) 为设计准绳(本清单的任务在其上落地)。
>
> 任务标签:`DJ`=主要靠 data-juicer 引擎 · `自研`=平台层自建 · `三方`=集成外部系统 · `前端`=仅前端工作。
> 优先级:`P0`=地基/阻塞项 · `P1`=核心需求 · `P2`=完整性/加分项。

---

## 一、现状基线

### 已打通(前端 + 真实后端 + 测试)
- 平台壳:登录(后端兼容层 `admin/ant.design`)、布局、菜单、明暗主题切换
- 需求 #1/#2/#3 **数据接入**(部分):数据源 CRUD + 测试连接、采集任务(进度**模拟**)、本地文件上传、AI 助手(`infer-schema`/`generate-task`/`qa`,启发式兜底 + OpenAI 兼容 LLM 可插拔)
- 后端资源:`datasources` / `ingest-tasks` / `uploads` / `ai` 四组 API,PostgreSQL 16 + Alembic
- chatbot 对话页(前端,接 `ai/qa`)

### 仅前端占位页(`PlaceholderPage`,无后端)
`security`(#4)、`quality`(#6)、`processing`(#7/#8/#9)、`data-tasks`(#10)、`lineage`(#11)、`annotation`(#12)、`datasets/list`(#13–19)、`datasets/presets`(#20)

### 完全未启动
- 需求 #5 访问安全(RBAC/审计)——当前仅 mock 登录,无真实鉴权
- **data-juicer 引擎接入**——后端任务进度是模拟推进(见 backend-design 第 37 行),引擎尚未真正执行

> 注:以上「完全未启动」是 **2026-06-08 基线**;data-juicer 引擎接入已于 06-08/06-11 完成(见 §二 P0)。#5 访问安全已于 **2026-06-15** 完成(RBAC + 审计,见 §B 与下方增量)。

### 2026-06-15 增量(本轮,均已部署到 10.60.1.60 + 推 dev)
- **算子市场**(清单原未单列):212 算子全量目录浏览/检索/分面(可运行 vs 推荐、资源路由 cpu/api_llm/gpu/hf_model/vllm),一级菜单 `/operators`。
- **#7 清洗 + #10 编排**:新增 `/processing/editor` **整页流水线编辑器**(算子库 + 拖拽有序步骤 + 逐步配参 + 实时 YAML 预览 + AI 生成),取代旧模态;清洗类算子已可选用并真跑 `dj-process`。
- **#6 质量评估**:升级为 `/quality/editor` 整页 + AI 推荐质量算子(仅 filter 类)。
- **AI**:接入 **DeepSeek `deepseek-v4-flash`**(替换启发式);新增 `POST /ai/generate-pipeline` 与 `/ai/generate-quality`(LLM 选算子 + 代码侧 sanitize 守门,失败回退启发式)。
- **部署**:整套 Docker Compose(PG + 后端含 DJ 引擎 + nginx 前端)上线 10.60.1.60,数据从 119 迁入,dev ↔ origin 同步。
- **#5 访问安全**:RBAC 真实登录(DB 用户表 + PBKDF2 口令 + HMAC 签名令牌,替换 mock)+ 角色门控(6 个写端点 `require_admin` + 前端 access,双层)+ 操作审计(中间件落 `audit_logs` + `GET /audit` + security 审计页);迁移 0006 种子 admin/user(口令 ant.design)。已部署 10.60.1.60 并 E2E 通过(admin 全权 / user 只读被拦 / admin 可登回归)。
- **#4 内容安全审核**:独立 `/content-safety` 审核任务(`type=review`)= LLM(DeepSeek)+ 内置/自定义敏感词 + PII(纯正则含 Luhn);命中**仅打标**产 `safety` 版本 + `review_findings`/报告页;迁移 0007。已部署 60 并 E2E(LLM 命中赌博、PII 五类、自定义/内置词全中、产打标版本)。
- **#18 外部 S3 托管 + MinIO**:60 上起独立 MinIO(S3 兼容,9000/9001,见 deploy/README);平台 s3 连接器升级真连 + 数据集列表「托管 S3 数据」向导,纯引用登记(origin=hosted,不拷贝),预览/加工/质量/审核按需拉取(产受管新版本、源不动),删源禁止、仅 admin 取消托管;迁移 0008。已部署 60 + E2E(真连 MinIO)。
- **修复**:`ray_*` 分布式算子归类为 needs_compute(不再误标「可运行」)。

---

## 二、P0 地基:data-juicer 引擎接入层 ⭐

> 需求 6/7/8/9 共同依赖此层。没有它,清洗/质量/合成/处理都只能停在占位。
> 数据模型按 [`03-架构设计.md` §1](03-架构设计.md) 的 Dataset/Version/Job/Lineage 核心落地。

- [x] `自研` 数据模型地基:建 `dataset` / `dataset_version`(不可变) / `job` / `job_input` 表(迁移 0002);**`ingest_task` 收编已完成**(迁移 0005,2026-06-11):任务保留为调度配置,每次运行产一条 `job(type=ingest)`,原 `ingest_runs` 表退役,采集产物血缘指向运行 job
- [x] `自研` 版本与血缘约定:版本不可变、Job 是唯一写入者;`job_input` + `produced_by_job_id` 边即血缘(#11 免费导出)
- [x] `DJ` `自研` 设计引擎调用边界:**已定子进程**(`dj-process`/`dj-analyze` --config,见 `services/engine.py`/`quality.py`,信号量限流)
- [x] `自研` 后端「YAML 配置生成器」:`engine.build_config`(路径统一正斜杠,兼容 Windows)
- [x] `自研` 真实**任务执行器**:`run_process_job`,状态机 pending→running→success/failed、run.log、产物路径(无中间进度百分比,demo 可接受)
- [x] `自研` 任务产物存储约定:`datasets_dir/<dataset_id>/v<n>/data.jsonl` + stats 文件挂 `stats_uri`
- [x] `自研` 算子目录服务:构建期快照 212 算子全量目录(`operators_catalog.json`,含参数 schema/分面)
- [x] `自研` **接入落地契约**:`services/landing.py`,任意源 → jsonl → 真实 `DatasetVersion`
- [x] `自研` **本地上传接成第一个真连接器**:上传即落地为受管资产(支持全部 #3 文件格式)
- [x] `DJ` 环境落地:mac 与 Windows 均装好并跑通(Windows 需 `/std:c++17` 编译 + fork 两处兼容修复)

---

## 三、按需求分组的任务

### A. 数据接入(#1/#2/#3)—— 连接器广度/增量(落地机制已上移至 §二 P0)
> 接入的"落地契约 + 第一条真实路径(本地上传)"是底座,见 §二。本节是在其之上**扩展连接器广度**;缺驱动的标"承诺支持"。
- [ ] `自研` #1 S3/OSS/OBS 对象存储真实拉取(替换数据源"测连即通过")
- [ ] `自研` #1 HDFS 拉取
- [ ] `自研` #1 数据库直连拉取:达梦/GoldenDB/kingbase/gaussdb/hologres/巨杉/hive/doris(按驱动可得性分批,缺驱动的标"承诺支持")
- [ ] `自研` #1 API 推送接收端点(承诺支持即可,先留 stub + 文档)
- [ ] `自研` #1 采集任务调度:cron 批量拉取(现 AI 已能生成 cron 表达式,缺真实调度器,如 APScheduler)
- [x] `自研` #1.5 **采集任务补"采集对象"(extract spec)**:已加 `ingest_tasks.extract`(迁移 0003);PostgreSQL 表/SQL 已支持,S3 前缀/HDFS 路径随对应连接器补
- [x] `自研` #1.5 **采集成功→统一落地为 DatasetVersion**:PostgreSQL 真实拉取已走 landing 契约落地(其余源随连接器补)
- [ ] `自研` #1.5 **修死字段**:前端 `filter` ↔ 后端 `filters` 名称不一致且"接受不持久化",过滤条件当前完全失效
- [ ] `自研` #1.2 **统一两条上传路径**:老"本地上传"(`UploadRecord` 仅文件记录)与新 `POST /datasets/upload`(真落地)语义统一,上传也走 landing 出口
- [x] `三方` #3 文档解析前置:markitdown 已接入**上传链路**(全部 #3 格式);采集链路待接
- [ ] `DJ` `自研` #2 时序、GIS 位置数据类型支持(自定义算子/字段约定)
- [ ] `自研` #2 多类型数据融合"统一语义"层(字段映射/schema 归一)

### B. 数据安全(#4/#5)
- [x] `自研` #4 内容安全审核(2026-06-15):独立 `type=review` 审核任务 = LLM(DeepSeek 黄赌毒政恐)+ 内置词表 + 自定义敏感词/正则;NSFW 图/视频(GPU)未做(承诺支持)
- [x] `自研` #4 命中后处置(2026-06-15):**仅打标**(产带 `safety` 字段新版本)+ 审核记录(`review_findings` 表 + `jobs.review_report` + `/content-safety` 报告页);删除处置本期未做
- [x] `自研` #4 PII 检测(2026-06-15):Presidio **思路**纯正则(身份证/手机/邮箱/银行卡+Luhn/IPv4),零三方依赖;完整 Presidio spaCy NER(姓名/地址)因服务器约束延后
- [x] `自研` #5 RBAC(2026-06-15):DB 用户表 + PBKDF2 口令 + HMAC 签名令牌(httpOnly cookie),替换 mock 登录;种子 admin/user(口令 ant.design)。**demo 级**:用户表带 `role` 字段(admin/user),非完整「用户/角色/资源」三表
- [x] `自研` #5 越权防护 + 操作审计日志(2026-06-15):6 个写端点 `require_admin`(datasets 删/批删/改、datasources 增/改/删)+ 前端 access 门控(双层);审计中间件落 `audit_logs` + `GET /audit`(admin)
- [x] `前端` security 页(2026-06-15):占位 → 真实操作审计日志列表(#5,ProTable + 过滤);#4 内容审核独立为 `/content-safety` 一级菜单页

### C. 数据质量评估(#6)—— data-juicer 主场 ✅(2026-06-11 完成;2026-06-15 升级:整页 `/quality/editor` + AI 推荐质量算子)
- [x] `DJ` 质量评估任务:`type=quality` Job 子进程跑 `dj-analyze`,Filter stats 逐条打分(不删行)+ Analyzer 分析报告,stats 路径回写版本 `stats_uri`
- [x] `自研` 逐条得分详情查看(GET `/dataset-versions/{id}/stats` 分页,与数据文件按行号对齐)
- [x] `DJ` `前端` 质量报告可视化(GET `/dataset-versions/{id}/quality-report` 聚合:均值/分位数/20 桶直方图;前端纯 div 直方图,未引图表库)
- [x] `自研` 阈值过滤"一键删除低质数据"→ 复用 POST `/jobs`(filter 算子带阈值)产新版本(冒烟:20 行 → 14 行 v2)
- [x] `前端` quality 页落地(任务列表 + 新建评估 + 报告/逐条得分/低质过滤三页签)

### D. 数据加工(#7 清洗 / #8 合成增强 / #9 处理)—— DJ 主场
- [x] `DJ` #7 清洗算子接入:不可见字符/规范空格/去乱码/繁简转换/去网页标识/去表情/去重(均有内置 Mapper/Deduplicator)——已进算子市场目录,经 `/processing/editor` 选用并真跑 `dj-process`(2026-06-15;注:作用于 `text` 字段,表格类数据需先做字段映射)
- [x] `自研` #7 清洗结果**预览 → 采纳 → 持久化**闭环(预览走 demo/采样,采纳产新版本)——✅ 编辑器「🔍 试跑样例」前 N 行跑 `dj-process` 智能对比加工前后(POST /jobs/preview,临时目录、不建版本),满意后「创建任务」持久化(2026-06-15)
- [ ] `DJ` #8 合成/增强 Mapper 接入;#8 数据蒸馏流程(Magpie 等方法论见 doc 04)
- [ ] `自研` #8 原始 vs 合成数据**区分标记**(字段约定 + 血缘)
- [ ] `DJ` #9 QA 对抽取(内置算子);#9 COT 数据生成(LLM Mapper,自定义)
- [x] `前端` processing 页:算子编排面板(见 E 段拖拽)——`/processing/editor` 整页编辑器(2026-06-15)

### E. 数据任务与血缘(#10/#11)
- [ ] `自研` #10 任务广场:处理/分析任务列表 + 详情(复用 ingest-tasks 模型扩展为通用 job)
- [ ] `自研` #10 任务操作:重跑/停止/删除(ingest 已有,推广到所有任务类型)
- [ ] `自研` #10 任务检索:按 ID/名称等
- [x] `前端` `自研` #10 **拖拉拽算子编排**:画布产物 = data-juicer YAML(依赖 P0 配置生成器 + 算子目录)——有序拖拽步骤 + 实时 YAML 预览 + AI 生成(DJ 线性模型,非自由画布;2026-06-15)
- [ ] `自研` #11 数据血缘:数据集 ← 来源任务 ← 涉及资产(数据源/算子配置/输入版本),保证可溯源可复现
- [ ] `前端` lineage 页:血缘图谱可视化

### F. 数据标注(#12)—— 三方为主
- [ ] `三方` 标注引擎选型与集成(Label Studio 方向,需求映射标"待验证")
- [ ] `自研` 单人/多人标注、智能预标注(LLM)+ 人工审核、任务自动/人工分配、交叉比例配置
- [ ] `自研` `前端` 标注进度、标注结果、可视化报告
- [ ] `前端` annotation 页落地

### G. 数据集仓库(#13–#20)—— 几乎全自研 ⭐
- [x] `自研` #13 元数据模型:固化字段(创建人/创建时间/版本/更新时间/最后变更人/归属/大小)只读 + 可变字段(名称/描述/类型/分级/分类/有效期)可编辑——`PATCH /datasets/{id}`(exclude_unset,显式 null 可清空)+ 详情抽屉「编辑」表单(2026-06-15;来源字段未单列,可由版本 origin 推断)
- [ ] `自研` #14 数据集打标 + 标签库 CRUD;`DJ` 内容分析自动打标(分类 Filter/LLM)
- [ ] `自研` #15 分级分类:敏感级别 × 业务类型双维度——🟡 两维度字段已可在元数据表单编辑(分级 public/internal/confidential、分类自由填),缺受控标签库/树与按分级分类筛选
- [x] `自研` #16 检索:元数据多条件(名称/创建人/时间/类型)——`GET /datasets` 过滤(同时作用 count+list)+ 列表搜索表单;标签维度待 #14;内容检索见下
- [ ] `自研` #16 内容检索(可上 embedding 向量检索,加分)
- [x] `前端` #16 数据集列表/卡片展示 + 数据预览(版本数据预览接口已通)
- [x] `自研` #17 版本模型:同一数据集多版本、版本不可变、独立元数据已落地(自研实现,未引 DVC/lakeFS)
- [x] `自研` #18 三方 S3 数据托管(2026-06-15):纯引用·按需拉取(origin=hosted,只存 s3:// 不拷贝)+ 物化解析器接入预览/加工/质量/审核(产受管新版本、源不动)+ 真实 S3(minio SDK)+ 托管登记/取消;删源禁止(hosted 删除 403,仅 admin 取消托管、绝不碰 S3)。已部署 60 + E2E(真连 MinIO)
- [ ] `自研` #19 操作:数据集级 + 数据级删除/批量删除;增删改查产新版本;共享 + 权限配置;下载/导出到本地或 S3;生命周期(有效期 + 到期清理)——🟡 删除/批量删除/元数据编辑已有,有效期字段可设;**共享权限、下载导出、到期清理未做**
- [ ] `自研` `DJ` #20 预置银行业数据集(每集 ≥2000 条):公开数据集筛选 + 合成补齐(行业数据集清单待补研)
- [x] `前端` datasets/list 页落地(列表+检索+详情+元数据编辑+版本+预览+删除);`datasets/presets` 仍为占位(#20)

---

## 四、补研事项(开工前需澄清/调研)

> 设计层面的待决策(ingest 收编、版本存储 DVC/lakeFS、connector 返回契约、算子目录来源、embedding 内容检索)统一记在 [`03-架构设计.md` §6](03-架构设计.md);本节只列需外部调研的事项。

- [ ] 数据库直连 8 种国产库的驱动可得性 → 区分"真支持"与"承诺支持"
- [ ] 版本/血缘工具实践(DVC / lakeFS)选型
- [ ] PII / 毒性检测引擎对比(Presidio 等)
- [ ] 银行业公开数据集清单(支撑 #20)
- [ ] 标注引擎(Label Studio)集成可行性验证

---

## 五、建议里程碑(粗排)

| 阶段 | 目标 | 主要任务 |
|------|------|----------|
| **M0 地基** | 引擎真正跑起来 | 第二节全部 P0 |
| **M1 处理闭环** | 清洗/质量看得见效果(DJ 主场) | C 段 + D 段(#6/#7) + 数据集仓库最小子集(#13/#17) |
| **M2 管理平台** | 数据集仓库 + 任务广场 + 血缘 | G 段主体 + E 段 |
| **M3 安全与高级加工** | 合规 + 合成/蒸馏 | B 段(#4/#5)+ D 段(#8/#9) |
| **M4 完整性/加分** | 标注、托管、预置集、多源接入 | F 段 + A 段补全 + #18/#20 |

> 排序原则:先打通 data-juicer 主场(投入产出比最高、评分占比大),再补自研平台层,最后做三方集成与加分项。

---

## 六、排期(带起止日期)—— AI-Coding 演示版,硬截止 2026-06-30

**排期前提**(任一不成立则截止日不保):
- 验收口径 = **评分演示版(POC)**:DJ 主场(清洗/质量/加工)+ 数据集仓库 + 任务/血缘 + 安全**真实跑通**;难对接连接器(8 国产库 / HDFS / S3 真实拉取)做**脚手架 UI+API+「承诺支持」文档**,符合需求"评分方式"列大量「承诺支持即可」的口径
- 人力:**1 人全栈 + AI Coding**,任务**串行**;AI 加速主要作用于纯码(前端页/CRUD/模型/schema),外部对接与决策不随之等比加速
- 起始 **2026-06-09**,工作日制,已扣 **06-19 端午**(放假 1 天)
- 环境就绪:data-juicer 运行环境 / PG / (可选) LLM key 开工即可用
- 评审/决策快速回,不阻塞串行;**全程零缓冲**

### 分段甘特(15 工作日)

| 起 | 止 | 天 | 任务段 | 对应需求 |
|------|------|:--:|--------|----------|
| 06-09 | 06-12 | 4 | 数据模型地基(dataset/version/job/job_input,ingest_task 收编)+ 接入落地契约 + 上传连接器(→DatasetVersion)+ 执行器/YAML 生成/算子目录 | 地基(P0 §二) |
| 06-15 | 06-16 | 2 | 质量评估 + 清洗(DJ 主场,复用执行器) | #6/#7 |
| 06-17 | 06-18 | 2 | 合成增强/蒸馏 + QA抽取/COT | #8/#9 |
| 06-22 | 06-22 | 1 | 数据集仓库 CRUD 全套(元数据/打标/分级/检索/版本/操作)——**压缩为 1 天,重保元数据+版本+检索,打标/分级/导出降级最小** | #13–19 |
| 06-23 | 06-24 | 2 | 任务广场 + 拖拽编排 + 血缘 | #10/#11 |
| 06-25 | 06-26 | 2 | 内容安全 + RBAC/审计 | #4/#5 |
| 06-29 | 06-29 | 1 | 接入补全:文档解析真实 + 连接器脚手架(承诺支持) | #1/#3 |
| 06-30 | 06-30 | 1 | 标注 + 预置集 + 全局联调/演示收尾 | #12/#20 |

> 合计 **15 工作日,2026-06-30 准时完工**(零缓冲)。M0 地基 4 天 / 数据集仓库压缩为 1 天(+1/-1 抵消,06-23 后日期不变)。

### 演示版 vs 生产版的范围取舍(诚实声明)

| 需求 | 6/30 演示版交付 | 留到生产版 |
|------|----------------|-----------|
| #1 接入方式 | 本地上传✅、S3/HDFS/8库 **脚手架+承诺支持** | 真实驱动联调、生产级重试/断点 |
| #3 文档解析 | PDF/Office→文本**真实**(markitdown) | 复杂版式/扫描件 OCR |
| #6 质量 / #7 清洗 / #8 合成 / #9 加工 | **真实跑通**(DJ 算子) | 大数据量 Ray 分布式、性能调优 |
| #12 标注 | 基础单/多人标注 + LLM 预标注 | Label Studio 深度集成、复杂审核流 |
| #18 S3 托管 / #20 预置集 | 托管脚手架;预置集**用 DJ 合成补齐 ≥2000 条** | 行业真实数据集采购/清洗 |
| #5 安全 | RBAC + 审计**真实** | 等保级加固、渗透测试 |

> ⚠️ **风险提示(fail loud)**:此排期零缓冲,任一段超时即顺延越界。
> - **M0 地基(06-09~06-12,4 天)**:塞了数据模型(4 表)+ 接入落地契约 + 上传连接器 + 执行器/YAML/算子目录。加 1 天后压力缓解,但仍是最关键一段。验收门槛:M0 末须同时满足「最简流水线跑通 + 上传能落地为真实 DatasetVersion」,否则立即重评后续范围。
> - **数据集仓库(06-22,压成 1 天)= 新的爆点**:#13–19 surface 很大(元数据/打标/分级/检索/版本/操作),1 天只够**重保元数据 + 版本 + 元数据检索**这条主线;打标/分级/导出/共享降级为最小可用或脚手架,内容检索(embedding)不进首版。若当天超时,优先砍这些降级项而非主线。
> - 兜底原则:任一段告急,优先保 DJ 主场 #6/#7/#8/#9(评分占比最大),把 #12 标注 / #20 预置集作为可砍项。

---

### 附:无截止约束的理想排期(参考)

若非 6/30 硬截止、按稳健节奏(含预览采纳闭环、前端精修、补研充分),单人串行约 **81 工作日(≈4 个月,完工 2026-09-29)**,分 M0 地基→M1 处理闭环→M2 管理平台→M3 安全与高级加工→M4 完整性 五个里程碑。两版差异即"演示版砍范围 + AI 加速纯码"换来的压缩,取舍见上表。
